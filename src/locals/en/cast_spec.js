import { mn } from "date-fns/locale";
import { ceil } from "xe-utils";

export default {
    table: {
        cast_code: "Cast Code",
        spec_code: "Spec Code",
        product_size: "Product Size",
        weight_kg: "Weight(kg)",
        semi_size: "Semi Size(mm x mm)",
    },
    hint: "Search by Cast Code or Spec Code",
}

